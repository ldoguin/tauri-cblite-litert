services:
  web-dev:
    name: Vite dev server
    description: React frontend on port 1420
    commands:
      start: cd /workspaces/tauri-cblite-litert && pnpm dev --host 0.0.0.0
      ready: curl -sf http://localhost:1420
